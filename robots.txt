User-agent: *
Allow: /
# Image proxies pass through to Lorcast/TCGplayer CDNs — no reason for
# crawlers to pull card art through our bandwidth.
Disallow: /img-proxy/
Disallow: /tcg-img-proxy/
# Standalone Swiss sim page (noindex meta too) — its public face is the
# Analytics "Swiss Odds" tab at /analytics?a=swiss, which IS in the sitemap.
Disallow: /lab/
Disallow: /swiss.html

Sitemap: https://packs.ink/sitemap.xml
