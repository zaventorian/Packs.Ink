# Local-only scraping stack (graded_run.ps1 / terapeak_*.py, etl_tcgcsv_backfill.py,
# probe_archive.py). Install with:  pip install -r scripts/requirements-scrape.txt
-r requirements.txt
py7zr>=0.21.0
# Terapeak scraper (terapeak_login.py + terapeak_scrape.py)
playwright>=1.45.0
selectolax>=0.3.21
