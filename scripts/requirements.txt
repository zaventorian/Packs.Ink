# What the GitHub workflows (ETL, catalog watch, discovery, Elo refresh,
# synthetic monitor) actually import. The Terapeak/TCGplayer scraper stack is
# in requirements-scrape.txt — it pulled a Playwright install into every CI
# job for scripts CI never runs.
requests>=2.31.0
python-dotenv>=1.0.0
